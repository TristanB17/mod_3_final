<br>
<% if @word_values.valid %>
  '<%= @word_values.word_name %>' is a valid word and its root form is '<%= @word_values.root %>'.
<% else %>
  '<%= @word_values.word_name %>' is not a valid word.
<% end %>
